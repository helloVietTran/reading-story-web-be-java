server:
  port: ${PORT:8080}
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://root:trmAKIhIVbVtfRdiMGkTaKYOsriztgui@mysql.railway.internal:3306/railway
    driverClassName: com.mysql.cj.jdbc.Driver
    username: ${MYSQLUSER:root}
    password: ${MYSQLPASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

  redis:
    host: ${REDISHOST:localhost}
    port: ${REDISPORT:6379}
    password: ${REDISPASSWORD}
    username: ${REDISUSER}
    timeout: 10000

app:
  brevo:
    apikey: ${BREVO_API_KEY}
    url: https://api.brevo.com
  cloudinary:
    cloud-name: ${CLOUDINARY_CLOUDNAME}
    api-key: ${CLOUDINARY_API_KEY}
    api-secret: ${CLOUDINARY_API_SECRET}
  jwt:
    secretKey: ${JWT_SECRETKEY:iamvietanh<3}
    access-duration: 3600 # in seconds
    refreshable-duration: 36000 # in seconds
  folder:
    avatar: "avatar_folder"
    chapter: "chapter_folder"
    story: "story_folder"
    avatar-frame: "avatar_frame_folder"
  admin-email: ${ADMIN_EMAIL:numberzero0909@gmail.com}
  frontend:
    url: ${FRONTEND_URL:http://localhost:5173}
